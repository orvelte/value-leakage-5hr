__pycache__/
*.pyc
.venv/
venv/

# Judge INPUTS are regenerable in seconds from raw/ + the frozen prompts in src/qual/judge.py.
# Judge OUTPUTS and raw generations are NOT: sampling uses seed=None by design (see the
# ENGINE_SEED note in sample.py), so rollouts cannot be reproduced from config.json. Track them.
runs/*/judge_inputs/
runs/*/trajectory_inputs/

# Raw activation tensors: 100MB and deterministically regenerable (teacher-forced, no sampling)
# via runs/internals_extract.py. The derived results JSONs beside them ARE tracked.
runs/internals/acts.pt
# same for the thinking-off set (runs/a7_nothink_extract.py), 200 x 64 x 5120
runs/internals/acts_nothink.pt

planning/
