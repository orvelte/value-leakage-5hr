__pycache__/
*.pyc
.venv/
venv/

# Judge INPUTS are regenerable in seconds from raw/ + the frozen prompts in src/qual/judge.py.
# Judge OUTPUTS and raw generations are NOT: sampling uses seed=None by design (see the
# ENGINE_SEED note in sample.py), so rollouts cannot be reproduced from config.json. Track them.
runs/*/judge_inputs/
runs/*/trajectory_inputs/
